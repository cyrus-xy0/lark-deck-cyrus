interface:
  display_name: "Deck Renderer"
  short_description: "Compile DeckJSON and render/package Cyrus H5 deck artifacts."
  default_prompt: "Use $deck-renderer to compile DeckJSON and generate the H5 deck artifacts."
